# Environment Configuration for Tough Nose Karate Backend
# Copy this file to .env and update the values according to your setup

# =============================================
# APPLICATION ENVIRONMENT
# =============================================
NODE_ENV=development
PORT=3001
API_PREFIX=api

# =============================================
# DATABASE CONFIGURATION - SUPABASE POSTGRESQL
# =============================================
# Supabase Session Pooler (recommended for production)
DB_HOST=
DB_PORT=
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

# Enable SSL for database connection (required for Supabase)
DB_SSL=true

# =============================================
# SECURITY CONFIGURATION
# =============================================
# JWT secret key (REQUIRED in production - minimum 32 characters)
# Generate a secure key: openssl rand -base64 32

# JWT token expiration time
JWT_EXPIRATION=1y
JWT_SECRET=JWT
# =============================================
# CORS CONFIGURATION
# =============================================
# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================
# RATE LIMITING
# =============================================
# Rate limiting configuration
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# =============================================
# GOOGLE SERVICES (OPTIONAL)
# =============================================
# Google Service Account configuration for Google Sheets integration
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GOOGLE_SERVICE_ACCOUNT_CLIENT_ID=your-client-id
# GOOGLE_SERVICE_ACCOUNT_PROJECT_ID=your-project-id
# GOOGLE_SERVICE_KEY_PATH=/path/to/service-account-key.json
# GOOGLE_SERVICE_ACCOUNT_KEY_ID=your-key-id

# =============================================
# PRODUCTION SPECIFIC SETTINGS
# =============================================
# When deploying to production, make sure to:
# 1. Set NODE_ENV=production
# 2. Generate and set a secure JWT_SECRET
# 3. Update CORS_ORIGINS to include your production domain
# 4. Set appropriate database credentials
# 5. Enable DB_SSL=true if your database supports it
# 6. Adjust THROTTLE_LIMIT based on your expected traffic

# Example production values:
# NODE_ENV=production
# JWT_SECRET=generated-secure-secret-key-at-least-32-characters-long
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# DB_SSL=true
# THROTTLE_LIMIT=100

# =============================================
# SUPABASE CONFIGURATION
# =============================================
# Supabase project configuration
SUPABASE_URL=URL
SUPABASE_ANON_KEY=your-anon-key-here
# SUPABASE_JWT_SECRET will be set in GitHub secrets for production

# =============================================
# GITHUB SECRETS FOR PRODUCTION DEPLOYMENT
# =============================================
# The following environment variables should be set as GitHub repository secrets:
#
# DATABASE_URL=postgresql://postgres.lyqncovqokixtsvzhaqc:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres
# SUPABASE_JWT_SECRET=your-supabase-jwt-secret-from-dashboard
# JWT_SECRET=your-production-jwt-secret
#
# GitHub Secrets Setup:
# 1. Go to repository Settings > Secrets and variables > Actions
# 2. Add each secret with the production values
# 3. Netlify will automatically use these during deployment