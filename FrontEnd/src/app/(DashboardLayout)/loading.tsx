import { Container } from '@mui/material';
import { IconKarate } from '@tabler/icons-react';

const Loading = () => {
  return (
    <Container>
      <div>
        <IconKarate />
      </div>
    </Container>
  );
};

export default Loading;
